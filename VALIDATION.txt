XIAO GPS Tracker Smart Motion v2.1 validation record

This record separates software/source checks from tests that require the
assembled hardware. It contains no measured current, runtime, or calibration
result.

FLASH STARTUP RECOVERY 2.1.2 (2026-09-17)

OBSERVED - User's serial trace repeats "FATAL: P25Q16H QSPI flash not detected".
           This locates the reported failure before metadata loading and BLE.
           The metadata-migration simulation below is a separate defect, not
           the startup stage failing in this trace.
PASS - Source/dependency review: firmware puts the flash in deep power-down;
       Adafruit SPIFlash 5.1.1 attempts JEDEC detection without first waking
       it. The RAM sleeping flag resets independently of externally powered
       flash, and Seeed core QSPI activation can time out while it sleeps.
PASS - Target build with Seeed nRF52 1.1.13, Seeeduino:nrf52:xiaonRF52840:
       172776 bytes program, 19380 bytes RAM.
PASS - Existing motion runtime and tracking profile host suites after patch.
PASS - test_flash_startup.py compiles the actual startup functions against a
       GPIO/flash model: original detection fails asleep; patched warm resets,
       cold boot and repeated resets pass, and absent flash still fails.
       Checks mode-0 0xAB framing, pin directions, CS byte boundary and wake
       delay before QSPI ownership. No erase/write API is available to the fake.
PASS - Independent review of wake timing, board pins and QSPI handover.
PENDING - On-device recovery following USB upload, with the battery connected.
          No physical tracker is attached to this build host. The trace does
          not by itself distinguish a sleeping chip from hardware/board issues.

ANDROID APK 2.1.1 BUILD (2026-09-16)

PASS - Installed a local headless ARM64 toolchain and completed a clean Gradle
       assembleDebug plus testDebugUnitTest: 44 tasks, all 11 tests passed.
       Uses API 36, Build Tools 35.0.0, OpenJDK 21, and AGP-matched Google AAPT2
       through QEMU user mode (Debian's ARM64 AAPT2 cannot read API 36 resources).
PASS - APK signature verified by apksigner; packaged version 2.1.1/code 22.
       This supersedes the earlier missing-SDK build blockers below.

BLUETOOTH STARTUP / SCAN FIX 2.1.1 (2026-09-16)

PASS - Reproduced against firmware 2.1.0 in the fake-flash host harness:
       valid v2 metadata plus an erase failure makes loadOwnerLock fail,
       which setup treats as fatal before Bluetooth starts.
PASS - Patched firmware loads valid v1/v2 metadata without erases or writes,
       preserving original flash bytes and owner/settings even when writes
       fail. An explicit successful settings save still upgrades to v3.
PASS - Existing firmware motion/profile host suites pass after the repair.
PASS - Target build with Seeed nRF52 1.1.13, Seeeduino:nrf52:xiaonRF52840:
       172488 bytes program, 19380 bytes RAM.
PASS - Android protocol/presentation JVM suite: 11 tests (Kotlin 2.3.0,
       JUnit 4.13.2), including tracking-profile compatibility.
PASS - Changed BleManager compiles against Android API 36 and AndroidX Core
       1.17.0. Independent source review confirms active-session checks and
       timeout cancellation for discovery, scan failure, stop and restart.
BLOCKED - Full Android Gradle test/APK build: no configured Android SDK.
          JVM tests and the direct BleManager compile do not cover real BLE.
ADDED - Checked BLE stack/service/advertising initialization and repeating
        fatal-stage output; periodic actual advertising/connection diagnostics.
PENDING - Physical confirmation of the reported connection problem: no board
          USB serial trace or radio observation is available in this workspace.
          The simulated startup failure is established; it is not proof that
          flash writes failed on the user's tracker.

POINT-TO-POINT PROFILE (2026-09-16)

PASS - Firmware 2.1.0 target build with Seeed nRF52 core 1.1.13,
       Seeeduino:nrf52:xiaonRF52840: 172040 bytes program, 19380 bytes RAM.
PASS - python3 firmware/tests/test_motion_runtime.py: actual scheduler/verifier
       bodies pass Continuous regressions plus Point-to-point movement without
       periodic GPS, exact 600-second stop, motion cancellation during arrival,
       five-minute timeouts, quiet-clock rollover, and zero timestamp handling.
PASS - python3 firmware/tests/test_tracking_profiles.py: actual persistence and
       GPS-save bodies pass v1/v2 migration, profile reload, owner-reset
       preservation, invalid configuration rejection, failed-save RAM rollback,
       fresh quality filtering, one departure/arrival, deadline ordering, and
       cancellation of a startup CAS12 probe before endpoint acquisition.
       GCC/G++ tests use fake peripherals; C++ compiler unpacked under /tmp.
PASS - Firmware review confirms 13-byte Smart Info v3, profile at byte 12,
       legacy 2-byte setter selects Continuous, v3 3-byte setter validates
       profile before saving. GPS record and INFO layouts remain unchanged.
PASS - Android ProtocolTest and TrackerPresentationTest: all 11 JUnit tests
       passed using Kotlin 2.3.0 compiler, JVM target 17, JDK 21, JUnit 4.13.2.
       This standalone run compiled GpsRecord, Protocol, TrackerPresentation,
       and both test classes; it does not compile the Activity or Android UI.
PASS - Android resource XML parses; independent review checked profile dialog,
       legacy-firmware gating, status presentation, and profile-aware readback.
BLOCKED - Full Android Gradle unit-test/APK build: temporary JDK runs Gradle,
          but task dependencies fail because the Android SDK is unavailable.
PENDING - Physical departure/arrival tests, smooth travel vs no acceleration,
          real GPS availability/accuracy, stop/resume near the 10-minute
          boundary, power-cycle persistence, and phone-to-board BLE readback.

Entries below record earlier releases/changes and their original test results.
Where behavior changed, the Point-to-point entry above supersedes them.

ANDROID LAUNCHER ICON REFRESH (2026-09-16)

PASS - Resource XML parses. Native SVG and Android vector use identical paths.
PASS - Adaptive resources for API 26 and explicit themed monochrome resources
       for API 33 resolve to the new foreground and background.
PASS - All ten standard/round density PNGs have the expected 48/72/96/144/192 px
       dimensions and transparent corners. Rendered artwork stays inside the
       adaptive 33 dp safe radius (measured maximum approximately 30.58 dp).
PASS - Visual inspection of the rendered 512 px preview and 48 px round icon.
PENDING - Android build and launcher/device checks; Java/JDK unavailable here.
          See docs/branding/README.md for design sources and regeneration.

ANDROID DASHBOARD REFRESH (2026-09-16)

PASS - Static resource checks: XML parsing, unique view IDs, single ScrollView
       child, bottom action buttons outside the scroll area, mode selector
       before conditional interval controls, 48 dp minimum button targets,
       and status text without line truncation.
ADDED - TrackerPresentationTest covers motion headlines, receiver-state
        independence, sensor faults, fallback priority, and Interval behavior.
BLOCKED - APK compilation and JUnit execution: Java/JDK is absent on this host.
PENDING - Device/emulator visual and gesture checks: light/dark themes, 320 dp
          screen width, landscape, large fonts, map pan/pinch vs page scrolling,
          empty/populated route, BLE reconnect, and mode switching.

MOTION REARM PATCH (2026-09-16)

PASS - Host checks execute extracted production scheduler/verifier bodies with
       a fake clock and peripherals, compiled with GCC. Focused checks cover
       latched-high IRQ recovery, jittered verification, three consecutive
       120-second cooldowns, stopped/restarted motion, failed-fix retry,
       millis rollover, stationary quiet-gap rejection, obsolete Interval
       deadline isolation, normal Interval wake, and I2C failure fallback.
       Run: python3 firmware/tests/test_motion_runtime.py
PASS - Actual MPU profile functions tested with fake registers: low-power
       settings, 20 Hz verification profile, and status-clear I2C failures.
PASS - The pre-patch HEAD fails the latched-high interrupt recovery test;
       the modified source passes it (test script --baseline reproduces).
PASS - Diagnostic build motion-rearm-1 compiled for
       Seeeduino:nrf52:xiaonRF52840, core 1.1.13, Arduino CLI 1.5.1,
       TinyGPSPlus 1.0.3 and Adafruit SPIFlash 5.1.1:
       171016 bytes program storage, 19380 bytes dynamic memory.
PENDING - Reproduce the reported one-fix-then-stuck failure and verify the
          corrected behavior on the user's assembled tracker. Host tests do
          not establish physical interrupt delivery or sensor calibration.

IMPLEMENTED / SOURCE-CONTRACT CHECKS

PASS - Release identifiers are firmware 2.0.0, Android 2.0.0, versionCode 20.
PASS - Wake modes are Interval = 0 and Smart = 1; the six persisted Interval
       values remain 60, 900, 1800, 3600, 7200, and 10800 seconds.
PASS - Smart mode retains the saved interval while selected; legacy
       CMD_SET_INTERVAL (0x05) selects Interval mode.
PASS - v2 protocol IDs are CMD_GET_SMART_INFO = 0x06,
       CMD_SET_SMART_CONFIG = 0x07, and RSP_SMART_INFO = 0x85.
PASS - CMD_SET_SMART_CONFIG has exactly the two-byte mode:u8,
       sensitivity:u8 payload; RSP_SMART_INFO has exactly 12 bytes in the
       documented field order.
PASS - The legacy 65-byte INFO response and existing log record layout remain
       compatible with earlier clients.
PASS - Metadata version 2 consumes two reserved bytes for wakeMode and
       smartSensitivity; version-1 migration defaults to Interval/Balanced and
       preserves owner identity and interval.
PASS - Owner reset clears ownership only and preserves route history, interval,
       wake mode, and Smart sensitivity. Clear-log preserves wake settings.
PASS - Smart source contract covers MPU candidate interrupt, 20 Hz / 5-second
       dynamic-acceleration verification, fresh UTC location acquisition, and
       cooldown/reconfirmation behavior.
PASS - GPS status distinguishes physical supply, receiver activity, UART/parser
       activity, and timed standby; no physical-GPS-power-off capability is
       claimed for the assembled wiring.

NOT PERFORMED / PENDING HARDWARE VALIDATION

PENDING - Inspect the physical assembly: 1000 mAh 1S LiPo to XIAO BAT/GND;
          MPU VCC/GND to 3V3/GND, SDA D4, SCL D5, INT D2, address 0x68; GPS
          VCC/GND to 3V3/GND, TX to D7, RX from D6; D1 unused.
PENDING - On the real MPU6050, verify WHO_AM_I, I²C address, critical register
          read-back, data-ready configuration, and the D2 interrupt.
PENDING - On the real GPS, verify 3.3 V operation, 9600-baud UART wiring,
          NMEA reception, valid RMC/GGA navigation, and fresh UTC fixes.
PENDING - Runtime CAS12 capability check: after normal NMEA, complete two
          controlled standby cycles, observing NMEA silence, resumption, fresh
          RMC/GGA, and subsequent valid navigation for each. Mark the capability
          unavailable if either cycle fails; do not continuously retry it.
PENDING - Measure receiver and whole-device current in Interval and Smart
          states, including BLE connected/disconnected and any timed-standby
          slice. Do not infer battery runtime from nominal capacity.
PENDING - Compare 30, 60, and 120-second standby slices for current and resume
          latency. Thirty seconds is a workaround candidate, not a measured
          optimum.
PENDING - Calibrate Smart sensitivity with an extended stationary run and
          representative walking, city, and smooth-driving runs. The 80/160/300
          mg preset values are starting values only.

BUILD/TOOLING STATUS

PASS - Firmware target compilation completed with Arduino CLI 1.5.1, the
       non-mbed Seeed nRF52 Boards core 1.1.13, FQBN
       Seeeduino:nrf52:xiaonRF52840, TinyGPSPlus 1.0.3, and Adafruit SPIFlash
       5.1.1. Result: 169880 bytes (20%) program storage and 19376 bytes (8%)
       dynamic memory. The temporary toolchain was outside this repository.
PENDING - Android unit-test/APK execution was attempted with Gradle and a
          temporary JDK 17, but stopped before compilation because the Android
          SDK path is unavailable. Protocol tests are present in app/src/test
          and still require an Android SDK-equipped environment to execute.
PENDING - The requested fake-clock firmware state-machine suite and end-to-end
          old-app/new-firmware compatibility tests still require a dedicated
          host-test harness or execution against instrumented hardware.

Until the pending checks are performed on the physical assembly, this release
must not claim measured power savings, battery duration, CAS12 support, or final
motion-threshold calibration.
